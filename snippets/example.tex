
local ls = require("luasnip")
local s = ls.snippet
local t = ls.text_node

return {
  tex = {
    s("doc", {
      t({
        "\\documentclass{article}",
        "\\usepackage[utf8]{inputenc}",
        "\\begin{document}",
        "",
        "\\end{document}",
      }),
    }),
  },
}
